name: CI

on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ master ]
    paths-ignore:
      - 'README.md'
      - 'LICENSE'
  pull_request:
    branches: [ master ]
    paths-ignore:
      - 'README.md'
      - 'LICENSE'

jobs:
  build-Ubuntu:
    name: "Build Appimage on Ubuntu"
    runs-on: ubuntu-latest

    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      - run: sudo apt update
      - run: sudo apt install libopengl-dev libopenal-dev libxrandr2 libgl-dev libflac8 libudev-dev libvorbis0a libvorbisenc2 libvorbisfile3 libogg-vorbis-decoder-perl
        
      - name: Configure the project with CMake
        run: cmake -B build -DCMAKE_INSTALL_PREFIX=AppDir/usr
        
      - name: Compile and install to AppDir
        run: make -C build install

      - name: Download linuxdeploy
        run: wget https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage && chmod +x linuxdeploy-x86_64.AppImage

      - name: Add linuxdeploy dependency
        run: sudo add-apt-repository universe; sudo apt install libfuse2

      - name: Create AppDir
        run: ./linuxdeploy-x86_64.AppImage --appdir AppDir

      - name: Create AppImage
        run: ./linuxdeploy-x86_64.AppImage --appdir AppDir --output appimage

      - name: Save AppImage
        uses: actions/upload-artifact@v4
        with:
          name: AppImage
          path: Game_of_life*
  build-Windows:
    name: "Build on Windows"
    runs-on: windows-latest

    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      - name: Configure the project with cmake
        run: cmake . -G "MinGW Makefiles" 

      - name: Compile
        run: mingw32-make

      - name: Save executable
        uses: actions/upload-artifact@v4
        with:
          name: Windows executable
          path: game-of-life.exe